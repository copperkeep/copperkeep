apiVersion: v2
name: copperkeep
description: Self-hosted platform for teaching programming — all execution client-side in WebAssembly
type: application

# version == appVersion, always. These ship as one thing; independent chart versioning
# is for charts that package someone else's software.
version: 0.1.2
appVersion: "0.1.2"

kubeVersion: ">=1.27.0-0"
home: https://github.com/copperkeep/copperkeep
sources:
  - https://github.com/copperkeep/copperkeep
keywords:
  - education
  - python
  - self-hosted
annotations:
  artifacthub.io/license: AGPL-3.0-only
