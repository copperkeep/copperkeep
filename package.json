{
  "name": "copperkeep",
  "private": true,
  "version": "0.1.6",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @copperkeep/web dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
